{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 3210",
    "generate": "NUXT_APP_BASE_URL=/secure/mg/ nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "sass": "^1.56.1"
  },
  "dependencies": {
    "@types/nodemailer": "^6.4.6",
    "nodemailer": "^6.8.0"
  }
}
