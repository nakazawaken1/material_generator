{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port 3210",
    "generate": "NUXT_APP_BASE_URL=/secure/material-interface/ nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/body-scroll-lock": "^3.1.0",
    "nuxt": "3.0.0",
    "sass": "^1.56.1"
  },
  "dependencies": {
    "@types/nodemailer": "^6.4.6",
    "body-scroll-lock": "^4.0.0-beta.0",
    "nodemailer": "^6.8.0"
  }
}
